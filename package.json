{
  "name": "@andcreations/common",
  "version": "0.1.4",
  "description": "Commons for other repositories",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist && rimraf node_modules",
    "rebuild": "rimraf dist && npm run build"
  },
  "author": "AndCreations",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "axios": "^0.24.0",
    "typescript": "^4.0.3"
  },
  "devDependencies": {
    "@types/node": "^14.11.5",
    "rimraf": "^3.0.2"
  }
}
